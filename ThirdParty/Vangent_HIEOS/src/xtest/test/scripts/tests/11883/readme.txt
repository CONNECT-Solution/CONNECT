R.a Submission Stored - All or Nothing

submit - Send a Register.a transaction including two documents.  The
metadata for one document is flawed (no URI attribute).  The transaction
must fail.

eval - use SQ to prove that nothing was stored.