<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<project name="JAXBBindingEJB" default="default" basedir="." 
     xmlns:ejbjarproject="http://www.netbeans.org/ns/j2ee-ejbjarproject/3">
<!--      xmlns:ejbjarproject2="http://www.netbeans.org/ns/j2ee-ejbjarproject/2">  -->
    <description>Builds, tests, and runs the project JAXBBindingEJB.</description>
<import file="../../../../build.lib.xml" />
    <import file="nbproject/build-impl.xml"/>
    <!--

    There exist several targets which are by default empty and which can be 
    used for execution of your tasks. These targets are usually executed 
    before and after some main targets. They are: 

      -pre-init:                 called before initialization of project properties
      -post-init:                called after initialization of project properties
      -pre-compile:              called before javac compilation
      -post-compile:             called after javac compilation
      -pre-compile-single:       called before javac compilation of single file
      -post-compile-single:      called after javac compilation of single file
      -pre-dist:                 called before archive building
      -post-dist:                called after archive building
      -post-clean:               called after cleaning build products
      -pre-run-deploy:           called before deploying
      -post-run-deploy:          called after deploying

    (Targets beginning with '-' are not intended to be called on their own.)

    Example of pluging an obfuscator after the compilation could look like 

        <target name="-post-compile">
            <obfuscate>
                <fileset dir="${build.classes.dir}"/>
            </obfuscate>
        </target>

    For list of available properties check the imported 
    nbproject/build-impl.xml file. 


    Other way how to customize the build is by overriding existing main targets.
    The target of interest are: 

      init-macrodef-javac:    defines macro for javac compilation
      init-macrodef-debug:    defines macro for class debugging
      do-dist:                archive building
      run:                    execution of project 
      javadoc-build:          javadoc generation 

    Example of overriding the target for project execution could look like 

        <target name="run" depends="<PROJNAME>-impl.jar">
            <exec dir="bin" executable="launcher.exe">
                <arg file="${dist.jar}"/>
            </exec>
        </target>

    Notice that overridden target depends on jar target and not only on 
    compile target as regular run target does. Again, for list of available 
    properties which you can use check the target you are overriding in 
    nbproject/build-impl.xml file. 

    -->
    
    <target name="wsimport-service-PropertyAccessor" depends="wsimport-init,wsimport-service-check-PropertyAccessor" unless="wsimport-service-PropertyAccessor.notRequired">
        <wsimport xendorsed="true" sourcedestdir="${build.generated.dir}/wsimport/service" extension="true" verbose="true" destdir="${build.generated.dir}/wsimport/binaries" wsdl="${basedir}/${meta.inf}/xml-resources/web-services/PropertyAccessor/wsdl/NhincComponentPropAccessor.wsdl" catalog="catalog.xml"/>

        <!-- Try to delete the artifacts that we do not want -->
        <echo message="XXXXXX  Deleting directory ${build.generated.dir}/wsimport/service/gov/hhs/fha/nhinc/common" />
        <delete dir="${build.generated.dir}/wsimport/service/gov/hhs/fha/nhinc/common" />
        <echo message="XXXXXX  Done deleting directory ${build.generated.dir}/wsimport/service/gov/hhs/fha/nhinc/common" />
        <!-- end of custom code -->


        <copy todir="${basedir}/${meta.inf}/wsdl/PropertyAccessor">
            <fileset dir="${basedir}/${meta.inf}/xml-resources/web-services/PropertyAccessor/wsdl/"/>
        </copy>

        <echo message="copy schemas" />
        <copy todir="${basedir}/${meta.inf}/wsdl/schemas/">
            <fileset dir="${basedir}/${meta.inf}/xml-resources/web-services/PropertyAccessor/schemas/" />
        </copy>
    </target>
    
    <target depends="init" if="no.dist.ear.dir" name="-deps-module-jar" unless="no.deps">
        <ant dir="${project.JAXBBindingLib}" antfile="build.xml" inheritall="false" target="jar"/>
    </target>
    <target depends="init" if="dist.ear.dir" name="-deps-ear-jar" unless="no.deps">
        <ant dir="${project.JAXBBindingLib}" antfile="build.xml" inheritall="false" target="jar"/>
    </target>
    
    <target depends="init" if="no.dist.ear.dir" name="deps-clean" unless="no.deps">
        <ant dir="${project.JAXBBindingLib}" antfile="build.xml" inheritall="false" target="clean"/>
    </target>
    
    <!-- Targets to remove the JAXB stuff -->
    
   <!--
    <target depends="init,deps-jar" name="-pre-pre-compile">
        <mkdir dir="${build.classes.dir}"/>
        <mkdir dir="${build.ear.classes.dir}"/>
    </target>

    <target depends="init,deps-jar,-pre-pre-compile,-pre-compile,-copy-meta-inf" if="have.sources" name="-do-compile">
        <ejbjarproject2:javac destdir="${classes.dir}"/>
        <copy todir="${classes.dir}">
            <fileset dir="${src.dir}" excludes="${build.classes.excludes}"/>
        </copy>
    </target>
    
    <target depends="init,deps-jar,-pre-pre-compile" name="-do-compile-single">
        <fail unless="javac.includes">Must select some files in the IDE or set javac.includes</fail>
        <ejbjarproject2:javac>
            <customize>
                <include name="${javac.includes}"/>
            </customize>
        </ejbjarproject2:javac>
    </target>
    -->
</project>
