<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<!-- By default, only the Clean and Build commands use this build script. -->
<!-- Commands such as Run, Debug, and Test only use this build script if -->
<!-- the Compile on Save feature is turned off for the project. -->
<!-- You can turn off the Compile on Save (or Deploy on Save) setting -->
<!-- in the project's Project Properties dialog box.-->
<project name="Patient-war" default="default" basedir=".">
  <description>Builds, tests, and runs the project Patient-war.</description>
  <import file="nbproject/build-impl.xml"/>
  <import file="../../../../../build.lib.xml"/>

  <target depends="init" if="no.dist.ear.dir" name="deps-module-jar" unless="no.deps">
    <property name="project.Patient-ejb.Absolute" location="${project.Patient-ejb}"/>
    <ant dir="${project.Patient-ejb.Absolute}" antfile="${project.Patient-ejb.Absolute}/build.xml" inheritall="false" target="dist"/>
  </target>
  
  <target depends="init" if="dist.ear.dir" name="deps-ear-jar" unless="no.deps">
    <property name="project.Patient-ejb.Absolute" location="${project.Patient-ejb}"/>
    <ant dir="${project.Patient-ejb.Absolute}" antfile="${project.Patient-ejb.Absolute}/build.xml" inheritall="false" target="dist-ear">
      <property location="${build.dir}" name="dist.ear.dir"/>
    </ant>
  </target>    
</project>
